import { registerRootComponent } from 'expo';

import App from './App';

// Point d'entrée Expo : enregistre le composant racine
// (App.js) comme composant principal de l'application.
registerRootComponent(App);
